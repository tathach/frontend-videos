<nav class="navbar navbar-dark p-2 sticky-top" style="background-color: #161616;">
  <div class="container d-flex justify-content-between align-items-center flex-nowrap">
    <a class="navbar-brand flex-shrink-0 me-3" href="/">
      {{#if settings.logo}}
        <img src="{{settings.logo}}" alt="Logo" height="60" />
      {{else}}
        <span class="text-white">Kuxx</span>
      {{/if}}
    </a>

    <form action="/search" method="post" class="position-relative flex-grow-1 form-search-control">
      <input
        class="form-control ps-5 search-form-control w-100"
        type="search"
        name="q"
        placeholder="Tìm kiếm video..."
        style="min-width: 100px;"
        {{#if keyword}}value="{{keyword}}"{{/if}}
      />
      <button type="submit" class="btn position-absolute search-btn p-0" style="top: 50%; left: 15px; transform: translateY(-50%);">
        <i class="bi bi-search text-secondary"></i>
      </button>
    </form>
  </div>
</nav>
{{> menu}}